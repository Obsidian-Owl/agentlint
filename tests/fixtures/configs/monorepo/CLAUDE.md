# Monorepo Root Config

This is the root-level configuration for the monorepo.

## Project Overview

A typical monorepo structure with multiple packages and services.

## Conventions

- Use TypeScript for all packages
- Follow ESM module format
- Test with Bun test runner

## Package Patterns

Each package should have its own CLAUDE.md if needed.
