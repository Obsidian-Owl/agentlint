/**
 * TUI Renderer Types
 *
 * Re-exports the ITuiRenderer interface from the main types module.
 *
 * @module tui/renderers/types
 */

export type { ITuiRenderer, AppProps, PermissionDecision } from '../types';
